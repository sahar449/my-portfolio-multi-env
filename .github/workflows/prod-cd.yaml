name: CD Prod

on:
  workflow_run:
    workflows: ["CD Staging"]
    types:
      - completed
    branches:
      - staging

permissions:
  id-token: write
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  AWS_REGION: us-west-2
  MGMT_CLUSTER: eksdemo-prod-cluster
  TARGET_CLUSTER: eksdemo-prod-cluster

jobs:
  deploy-prod:
    name: Deploy to Production
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ github.token }}

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: github-actions-cd-prod

      # ── Promote images from staging ECR to prod ECR ──────────────────────
      - uses: aws-actions/amazon-ecr-login@v2

      - name: Promote images staging → prod
        env:
          ECR: ${{ secrets.AWS_ACCOUNT_NUMBER }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com
          IMAGE_TAG: ${{ github.sha }}
        run: |
          for service in frontend backend; do
            docker pull  "${ECR}/${service}-staging:latest"
            docker tag   "${ECR}/${service}-staging:latest" "${ECR}/${service}-prod:${IMAGE_TAG}"
            docker tag   "${ECR}/${service}-staging:latest" "${ECR}/${service}-prod:latest"
            docker push  "${ECR}/${service}-prod:${IMAGE_TAG}"
            docker push  "${ECR}/${service}-prod:latest"
          done

      # ── Connect to mgmt cluster (where ArgoCD lives) ──────────────────────
      - name: Connect to mgmt cluster
        run: aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name ${{ env.MGMT_CLUSTER }}

      - name: Install ArgoCD via Helm
        run: |
          helm repo add argo https://argoproj.github.io/argo-helm
          helm repo update
          helm upgrade --install argocd argo/argo-cd \
            --namespace argocd \
            --create-namespace \
            --version 7.8.23 \
            --wait \
            --timeout 10m \
            -f ArgoCD/argocd-server-values.yaml

      - name: Install ArgoCD CLI
        run: |
          curl -sSL -o /usr/local/bin/argocd \
            https://github.com/argoproj/argo-cd/releases/download/v2.11.0/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      # ── Update image tag in Git ────────────────────────────────────────────
      - name: Update image tag in Git
        run: |
          IMAGE_TAG="${{ github.sha }}"
          ECR_FRONTEND="${{ secrets.AWS_ACCOUNT_NUMBER }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/frontend-prod"
          ECR_BACKEND="${{ secrets.AWS_ACCOUNT_NUMBER }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/backend-prod"

          wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/download/v4.44.1/yq_linux_amd64
          chmod +x /usr/local/bin/yq

          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git pull --rebase

          yq e ".app.image.repository = \"${ECR_FRONTEND}\"" -i ArgoCD/frontend/helm_chart/values.prod.yaml
          yq e ".app.image.tag = \"${IMAGE_TAG}\"" -i ArgoCD/frontend/helm_chart/values.prod.yaml
          yq e ".app.image.repository = \"${ECR_BACKEND}\"" -i ArgoCD/backend/helm_chart/values.prod.yaml
          yq e ".app.image.tag = \"${IMAGE_TAG}\"" -i ArgoCD/backend/helm_chart/values.prod.yaml

          git add ArgoCD/frontend/helm_chart/values.prod.yaml ArgoCD/backend/helm_chart/values.prod.yaml
          git diff --staged --quiet || git commit -m "cd: update prod image tag ${IMAGE_TAG}"
          git push

      # ArgoCD detects the git change and syncs to the prod cluster
      - name: Apply ArgoCD prod apps
        run: kubectl apply -f ArgoCD/prod/root-app.yaml

      - name: Wait for apps to be healthy
        run: |
          for i in $(seq 1 40); do
            FRONTEND=$(kubectl get application frontend-prod -n argocd \
              -o jsonpath='{.status.health.status}' 2>/dev/null || echo "NotFound")
            BACKEND=$(kubectl get application backend-prod -n argocd \
              -o jsonpath='{.status.health.status}' 2>/dev/null || echo "NotFound")
            echo "Attempt $i/40 — frontend: $FRONTEND, backend: $BACKEND"
            [ "$FRONTEND" = "Healthy" ] && [ "$BACKEND" = "Healthy" ] && break
            [ "$i" -eq 40 ] && exit 1
            sleep 15
          done
