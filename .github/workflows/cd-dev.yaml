name: CD Dev

on:
  workflow_run:
    workflows: ["CI Dev"]
    types:
      - completed
    branches:
      - dev

jobs:
  deploy-dev:
    name: Deploy to Dev
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment: dev
    steps:
      - uses: actions/checkout@v4

      # Replace with real steps when EKS is ready:
      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v4
      # - name: Login to ECR
      #   uses: aws-actions/amazon-ecr-login@v2
      # - name: Push to ECR
      #   run: docker push $ECR_URL/frontend:${{ github.sha }}
      # - name: kubectl apply to dev cluster

      - name: Deploy to dev
        run: echo "Deploying frontend+backend ${{ github.sha }} to dev"
